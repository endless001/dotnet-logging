kind: pipeline
type: docker
name: default

steps:
- name: build
  image: docker/compose
  commands:
    - docker-compose -f docker-compose.yml up -d
  volumes:
    - name: docker_socket
      path: /var/run/docker.sock

volumes:
    - name: docker_socket
      host:
        path: /var/run/docker.sock